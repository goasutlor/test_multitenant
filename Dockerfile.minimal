FROM node:18-alpine

WORKDIR /app

# Copy minimal files
COPY server.js .
COPY package-minimal.json package.json

# Install dependencies
RUN npm install

# Expose port
EXPOSE 8080

# Start server
CMD ["node", "server.js"]
